import { DiferredModeUsecases } from './diferred-mode-usecases';

describe('DiferredModeUsecases', () => {
  it('should create an instance', () => {
    expect(new DiferredModeUsecases()).toBeTruthy();
  });
});
